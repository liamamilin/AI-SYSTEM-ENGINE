# 本地 Ollama（默认，live 冒烟用）
OLLAMA_BASE=http://localhost:11434
LLM_MODEL=qwen3.5:9b-mlx
EMBED_MODEL=bge-m3

# 云端 OpenAI-compatible 供应商（可选；只改三个变量，代码零改动）
# LLM_BASE_URL=https://api.example.com/v1
# LLM_API_KEY=sk-xxx
# LLM_PROVIDER=openai

# Eval / Run 开关
RUN_LIVE_TESTS=0
EVAL_MODE=mock
