[project]
name = "structured-output-demo"
version = "0.1.0"
description = "Chapter 7: reliable structured output from LLM (schema validation + repair)"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.7,<3",
    "openai>=1.40",
]

[dependency-groups]
dev = [
    "pytest>=8",
    "pytest-asyncio>=0.23",
]

[build-system]
requires = ["uv_build>=0.7"]
build-backend = "uv_build"

[tool.uv.build-backend]
module-name = "structured_output"

[tool.pytest.ini_options]
asyncio_mode = "auto"
